import { footerContainer, footerTextStyle } from "./Footer.css"

export const Components = () => {
  return (
    <div className={footerContainer}>
      <span className={footerTextStyle}>Vista | All Right Reserved.</span>
    </div>
  )
}
